import SwiftUI

struct ContentView: View {
    var body: some View {
        //step 1: make the variables
        var clock: Duration = .seconds(1)
        var	 minute = 0
        var hour = 0
        VStack {
            //step 2: make the loop
            
        }
    }
}
